# =============================================================
# FIREBASE & GOOGLE SHEETS CONFIG — Copy to .env and fill in
# =============================================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in your values (never commit .env to Git)
# 3. For Vercel: add these in Project Settings → Environment Variables
# =============================================================

# Firebase (from Firebase Console → Project Settings)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Firebase Cloud Messaging — VAPID key (Project Settings → Cloud Messaging)
VITE_FIREBASE_VAPID_KEY=

# Google Sheets — full Web App URL (Apps Script → Deploy → Web app)
VITE_GOOGLE_SHEETS_URL=
